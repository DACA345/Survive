#pragma once

/**
 * @brief This struct describes the effect on the user's bars.
 * 
 * .
 */
struct Effect
{
    int healthBar;
    int thirstBar;
    int hungerBar;
    int moraleBar;
    int energyBar;
};