#pragma once
#include "../scalablewidget.h"

class NewGameMenu : public ScalableWidget
{
    Q_OBJECT;

    public:
        NewGameMenu(QWidget *parent = nullptr);
        ~NewGameMenu();

    private:

};